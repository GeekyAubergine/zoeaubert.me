---
layout: page
title: 'Referrals'
descrption: 'All my referral links'
---
{% for referral in referrals %}
    <div class="mb-8">
        <div class="flex flex-row items-baseline text">
            <h3 class="mr-1 text-xl font-normal">{{referral.name}}</h3> - 
            <a href={{referral.url}} target="_blank" rel="nofollow">
                <span class="ml-1 link">{{referral.url}}</span>
            </a>
        </div>
        <p class="secondary">{{ referral.description }}</p>
    </div>
{% endfor %}