---
layout: page
---

<p class="text mt-4 leading-[1.6rem] sm:mt-6 sm:leading-6">Hi there, I’m a software developer from Jersey, living in Portsmouth, working at <a href="https://radweb.co.uk" target="_blank" rel="noopener">Radweb</a> on <a href="https://inventorybase.co.uk" target="_blank" rel="noopener">InventoryBase</a> and other projects, primarily focusing on app development.</p>
<h2 class="mainPageHeading">Status</h2>
<div class="flex my-4">
    {% if statuslol.fluentEmoji %}
        <img src="{{ statuslol.fluentEmoji.src }}" alt="{{ statuslol.fluentEmoji.alt }}" class="w-12 h-12 mr-2 m-0 p-0"/>
    {% else %}
        <div class="flex w-12 h-12 mr-2 m-0 p-0 justify-center items-center">
            <p class="text-4xl">{{ statuslol.emoji }}</p>
        </div>
    {% endif %}
    <div class="flex flex-col h-12 justify-center">
        <p>{{ statuslol.content | linkifyMarkdown | safe }}</p>
        <a href="{{ statuslol.link }}" class="secondary underline_custom" target="_blank" rel="noopener">{{statuslol.relativeTime}}</a>
    </div>
</div>
<p class="secondary">See previous status' at <a href="https://geekyaubergine.status.lol" target="_blank" rel="noopener" class="secondary">status.lol</a>
</p>
<a href="/blog" class="link">
    <h2 class="mainPageHeading">Marco Blogs</h2>
</a>
{%- for blogPost in collections.recentPosts %}
    {% include "blogPost.njk" %}
{% endfor %}
<a href="https://geekyaubergine.com" target="_blank" rel="noopener" class="link">
    <h2 class="mainPageHeading">Micro Blogs</h2>
</a>
{%- for micro in microblog %}
    <div class="mt-2 mb-6">
        <a href="{{ micro.url }}" target="_blank" rel="noopener" class="link">
            <time class="link" datetime="{{ micro.date }}">
                {{ micro.date }}
            </time>
            {% if micro.title %}
                - {{ micro.title }}
            {% endif %}
        </a>
        <p class="text my-1">{{ micro.summary }}</p>
    </div>
{% endfor %}
<h2 class="mainPageHeading">Socials</h2>
<div class="mt-4">
    {% for social in socials %}
        <div class="flex mb-3 last-of-type:mb-0">
            <a class="link text-x" href="{{ social.link }}" target="_blank" rel="me">{{ social.name }}</a>
            <span class="text ml-1 sm:ml-2">-</span>
            <span class="text ml-1 sm:ml-2">{{ social.description }}</span>
        </div>
    {% endfor %}
</div>