<nav class="flex flex-1 flex-col width-control mx-auto justify-center items-baseline mb-4 md:flex-row md:justify-between">
    <div class="flex w-full flex-row items-center justify-between my-2 md:justify-start">
        <a class="text-3xl h-full text-left md:text-4xl" href="/">
            <p>Zoe Aubert</p>
        </a>
        {% include "themeToggle.njk" %}
    </div>
    <div class="flex w-full justify-between mt-2 md:justify-end">
        {% for link in navlinks %}
            <a
                class="link no-underline md:ml-2"
                href="{{ link.url }}"
                target="{{ link.target }}"
                rel="{{ link.rel }}"
            >
                {{ link.name }}
            </a>
        {% endfor %}
    </div>
</nav>