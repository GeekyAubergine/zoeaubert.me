---
layout: page
---
<div class="content">
    {{ about | mdToHtml | safe}}
</div>
<a href="/blog" class="link">
    <h2 class="mainPageHeading">Blog Posts</h2>
</a>
{%- for postId in blogposts.recentPostsOrder %}
    {% set post = blogposts.posts[postId] %}
    {% include "blogPost.njk" %}
{% endfor %}
<a href="/timeline" class="link">
    <h2 class="mainPageHeading mt-12">Recent Posts</h2>
</a>
{# {%- for postId in micros.recentPostsOrder %}
    {% set post = micros.posts[postId] %}
    <article class="my-2">
        <a href="{{ post.slug }}">
            <time class="link" datetime="{{ post.date }}">
                {{ post.date | formatDateTime }}
            </time>
        </a>
        <div class="my-1">
            {{ post.excerpt | mdToHtml | safe }}
        </div>
        <div class="flex flex-wrap my-0 py-0">
            {% if post.type == 'blogPost' or post.type == 'microblog' or post.type == 'mastodon' or post.type == 'micro' %}
                {% for tag in post.tags %}
                    <a href="/tags/{{ tag | slug }}" class="link secondary mr-2 my-0 py-0">
                    #{{ tag }}
                    </a>
                {% endfor %}
            {% endif %}
        </div>
    </article>
{% endfor %} #}
{%- for entityId in timeline.recentEntityOrder %}
    {% set post = timeline.entities[entityId] %}
    {% include "timelinePost.njk" %}
{% endfor %}
{# <hr class="mx-0"> #}
<h2 class="mainPageHeading mt-12">Socials</h2>
<div class="mt-4">
    {% for social in socials %}
        <div class="flex mb-1 last-of-type:mb-0">
            <a class="link" href="{{ social.link }}" target="_blank" rel="me">{{ social.name }}</a>
            <span class="text mt-0 mb-0 ml-1 sm:ml-2">-</span>
            <span class="text mt-0 mb-0 ml-1 sm:ml-2">{{ social.description }}</span>
        </div>
    {% endfor %}
</div>